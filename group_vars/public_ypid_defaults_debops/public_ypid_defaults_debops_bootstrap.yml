---
# vim: foldmarker=[[[,]]]:foldmethod=marker

## I have not seen a Debian without python installed yet.
## Does not work obviously because no facts are available at this point.
# bootstrap__raw: '{{ False if (ansible_distribution|d("SomethingElse") == "Debian") else True }}'
